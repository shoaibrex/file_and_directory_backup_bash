Created this `bash` script for learning. This scriot takes two arguments: first is the source file/directory path and second is the backup file/directory path

### Usage:

`bash backup_script.sh SOURCE_PATH BACKUP_PATH`
